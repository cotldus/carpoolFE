version: '3.8'
services:
  web:
    container_name : fe-carpool
    build:
      context: ./
      target: runner
    volumes:
      - .:/app
    command: npm run dev
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: development